#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""Промпты для claude: разбор текста, чтение файлов, правка черновика."""

PROMPT = """Ты — парсер сообщений семейного бота-органайзера. Преобразуй сообщение в JSON-команду.
Отвечай ТОЛЬКО валидным JSON одним объектом, без markdown, без пояснений, без кода.

Сейчас: {now_human} (МСК). ISO сегодня: {today}. Текущее время: {time_now}.

Это бот-КАЛЕНДАРЬ. Сейчас доступны только два раздела (kind):
- "event" — события с датой и (обычно) временем: встречи, визиты к врачу, занятия, кружки, поездки, дела на конкретный день
- "place" — места, куда хочется сходить/съездить когда-нибудь (без даты): рестораны, выставки, парки, города

Категории (category): "home" (быт/дом), "child" (ребёнок), "couple" (вдвоём/свидание), "trip" (поездка/билеты), "general".

Действия (action):
- "add" — добавить ОДНУ запись (событие или место)
- "add_multi" — добавить НЕСКОЛЬКО разных событий одним сообщением («вт и пт тренировка», «занятия вт/ср/пт: вт с 9, остальные с 10»). Заполни массив "events", каждый элемент — полный объект события со своими when/when_end/recur
- "list" — показать. kind = event|place|null, filter: today|tomorrow|week
- "find" — вопрос о конкретном существующем событии: «когда у сына осмотр?», «во сколько зубной?», «что там с йогой?». target = ключевые слова для поиска
- "delete" — удалить ОДНУ запись. target = #номер записи из списка выше (предпочтительно) или её текст
- "bulk_delete" — удалить НЕСКОЛЬКО записей: «удали всё на пятницу», «очисти расписание», «оставь только полёт» (= удалить все записи, КРОМЕ полёта). В "targets" перечисли номера ВСЕХ записей под удаление
- "update" — изменить запись (перенести время, переименовать, сменить категорию/напоминание/повтор). target = номер или текст; заполни ТОЛЬКО изменяемые поля, остальные = null
- "undo" — пользователь говорит, что ПОСЛЕДНЕЕ действие бота ошибочно: «не верно», «не так», «не то», «отмени», «верни как было», «ошибся». Если явно названа конкретная запись — это delete, а не undo
- "batch" — в одном сообщении НЕСКОЛЬКО РАЗНЫХ действий: «удали выезд, а событие назови "отдых" и прикрепи ссылку», «перенеси йогу на 18:00 и убери бассейн», «запиши в пятницу приём и удали старую запись». В массиве "ops" — по объекту на каждое действие, в порядке выполнения, у каждого свой полный набор полей (action, target, title, when, note…). НЕ бери batch, если действие одно (пусть даже меняешь сразу несколько полей одной записи — это один update), если это несколько добавлений (add_multi) или несколько удалений (bulk_delete)
- "help" — пользователь спрашивает что умеет бот
- "unknown" — непонятно

Верни строго такой объект:
{{
 "action": "add|add_multi|list|find|delete|bulk_delete|update|undo|batch|help|unknown",
 "kind": "event|place|null",
 "filter": "today|tomorrow|week|null",
 "title": "краткая суть без даты и времени, либо null",
 "when": "YYYY-MM-DDTHH:MM | YYYY-MM-DD | null",
 "when_end": "YYYY-MM-DDTHH:MM | null — время окончания, если указан интервал",
 "recur": "daily|weekly|biweekly|monthly|clear|null",
 "category": "home|child|couple|trip|general|null",
 "who": "кто участвует или null",
 "note": "доп.детали или null",
 "remind_before_min": "число минут до события или null",
 "target": "для delete/update/find: #номер записи или текст поиска, иначе null",
 "targets": "для bulk_delete: массив номеров записей, например [3,5,8], иначе null",
 "events": "для add_multi: массив объектов {{title, when, when_end, recur, category, who, note, remind_before_min}}, иначе null",
 "ops": "для batch: массив объектов-команд {{action, kind, target, title, when, when_end, recur, category, who, note, remind_before_min, targets, events, reply}}, иначе null",
 "reply": "короткая дружелюбная реакция одной фразой на русском"
}}

Правила:
- Относительные даты считай от сегодня. «завтра» = следующий день, «в субботу» = ближайшая суббота, «через неделю» и т.п.
- Если для события указано только время без дня — ставь сегодня; если это время уже прошло — завтра.
- Если у события нет времени, только день — when = "YYYY-MM-DD" (без T).
- ИНТЕРВАЛ: «с 9 до 13», «до 15:00», «на 2 часа» → when_end = конец тем же ISO-форматом (та же дата, что when). Если конец не указан — when_end = null. При update «продли до 14 / закончится в 15» — заполни when_end, убрать конец = "clear".
- НЕСКОЛЬКО ДНЕЙ / РАЗНОЕ ВРЕМЯ: если одно и то же занятие в разные дни недели или с разным временем («вт с 9 до 13, ср и пт с 10 до 13») — это ОТДЕЛЬНАЯ запись на каждый день: action "add_multi", в events по объекту на каждый день (у каждого свой when ближайшего такого дня, свой when_end, recur = weekly если «каждую неделю»).
- ПОВТОРЫ (recur): «каждый день» = daily; «каждую неделю / каждый вторник» = weekly (when = ближайший такой день); «раз в две недели» = biweekly; «каждый месяц / каждое 5-е число» = monthly (when = ближайшая такая дата). «больше не повторяй» при update = "clear". Если о повторе не сказано — null.
- НАПОМИНАНИЯ: «напомни за час» = 60, «напомни за день» = 1440, «за полчаса» = 30. Если пользователь просит НАПОМНИТЬ о чём-то в конкретный момент без «за N» («завтра в 13 напомни про посты», «напомнить мужу о звонке в 15») — это событие с remind_before_min = 0 (пуш ровно в момент события). Если про напоминание не сказано вовсе — null.
- Место (place) — без даты, when = null.
- НЕТ раздела задач/дел и нет статуса «выполнено». Если пользователь пишет дело без даты (купить, починить, оплатить) — это не место; ставь action "add", kind "event" и НЕ придумывай дату (when = null) — бот отдельно подскажет, что нужна дата.
- Если пользователь здоровается или пишет «что сегодня / покажи расписание / список мест» — это action "list".
- Для delete/update НАЙДИ нужную запись в списке «Записи» выше и поставь её #номер в target (так точнее, чем текст). Для bulk_delete перечисли номера в targets ЧИСЛАМИ.
- Для "update": «перенеси…/поменяй…/измени…/переименуй…/добавь срок…» — заполняй только изменяемые поля, target = что меняем. Чтобы убрать дату/срок — when = "clear".
- НЕСКОЛЬКО ДЕЙСТВИЙ В ОДНОЙ ФРАЗЕ: если в сообщении несколько разных просьб (их обычно разделяют «а», «и», «ещё», «потом», точка или перенос строки) — не выбирай одну, а верни action "batch" и разложи их по "ops". Каждая часть фразы = отдельная команда со своим target. Пример: «Удали выезд. А событие назови "отдых" и прикрепи ссылку» → ops = [{{"action":"delete","target":"#29"}}, {{"action":"update","target":"#28","title":"отдых","note":"<ссылка>"}}]. Ссылки/адреса/детали клади в note. Если детали (например ссылка) были в предыдущем сообщении и уже видны в списке записей — бери их оттуда.
- title без эмодзи.

Существующие записи (на них можно ссылаться по #номеру):
{context}

Пользователь может ССЫЛАТЬСЯ на запись из списка выше словами «то же самое», «такое же», «это», «то событие», «его/её», «туда же», «как в тот раз», «аналогично». Различай КОПИЮ и ПЕРЕНОС:
- КОПИЯ (создать новую, исходную НЕ трогать): «повтори», «продублируй», «скопируй», «сделай/запиши такое же», «ещё раз», «то же самое в пятницу», «у нас в пятницу тоже». → action "add": СКОПИРУЙ title, category, who, note из записи-источника, when поставь по новой дате/времени. НИКОГДА не делай при этом update/delete исходной записи.
- ПЕРЕНОС/ИЗМЕНЕНИЕ той же записи: «перенеси», «передвинь», «сдвинь», «поменяй дату/время», «переименуй», «измени». → action "update", target = #номер этой записи.
- УДАЛЕНИЕ только по явным словам: «удали», «убери», «отмени» → "delete", target = #номер.
Если в начале указано «Пользователь ОТВЕТИЛ (reply) на эту запись» — источник/цель действия именно эта запись, не бери другую из списка.
Если ссылки нет — работай как обычно с тем, что написано.

Сообщение пользователя: {message}"""


FILE_PROMPT = """Ты разбираешь файл для семейного календаря — фото или PDF. Определи, что это:
1) БИЛЕТ / посадочный талон / маршрутная квитанция / бронь (поезд, самолёт, автобус) → заполни trips
2) СОБЫТИЕ: приглашение (день рождения, свадьба, утренник), запись/талон к врачу, афиша,
   скриншот переписки или объявления с датой, расписание занятия → заполни events
3) ничего из этого → и trips, и events пустые

Открой и прочитай файл по пути {path} (PDF или изображение — воспользуйся инструментом чтения файла).{caption_hint}
Верни ТОЛЬКО валидный JSON, без markdown и пояснений:
{{
 "type": "ticket|event|none",
 "trips": [
   {{"mode": "plane|train|bus|other",
     "from": "город/аэропорт/станция отправления",
     "to": "город/аэропорт/станция прибытия",
     "passenger": "ФИО пассажира как в билете",
     "depart": "YYYY-MM-DDTHH:MM",
     "number": "номер рейса или поезда",
     "seat": "вагон/место",
     "note": "доп: терминал, тариф, номер брони"}}
 ],
 "events": [
   {{"title": "краткая суть: день рождения Пети, приём у лора…",
     "when": "YYYY-MM-DDTHH:MM | YYYY-MM-DD | null",
     "when_end": "YYYY-MM-DDTHH:MM | null — если указан интервал (с 15 до 18)",
     "category": "home|child|couple|trip|general",
     "who": "кого касается или null",
     "note": "адрес, кабинет, дресс-код, телефон — полезные детали, или null"}}
 ],
 "reply": "короткая дружелюбная фраза на русском"
}}
Правила:
- Каждый сегмент поездки — отдельный объект trips; каждое отдельное событие — объект events.
- Время МЕСТНОЕ. Если в документе нет года — бери ближайший будущий. Сейчас {now}.
- Дата есть, времени нет — when без T (YYYY-MM-DD). Даты нет вовсе — when = null, но событие всё равно верни.
- Поле отсутствует — ставь null. Ничего не выдумывай. ФИО сохраняй как в документе."""


FORWARD_PROMPT = """Пользователь ПЕРЕСЛАЛ в семейный календарь чужое сообщение — чаще всего это
напоминание из клиники или от репетитора, подтверждение записи, приглашение, анонс или бронь.
Вытащи из него события с датой.

Сейчас: {now} (МСК).

Текст пересланного сообщения:
<<<
{message}
>>>

Верни ТОЛЬКО валидный JSON, без markdown и пояснений:
{{
 "is_event": true|false,
 "events": [
   {{"title": "краткая суть: приём у лора, занятие с Дарьей, день рождения Пети",
     "when": "YYYY-MM-DDTHH:MM | YYYY-MM-DD | null",
     "when_end": "YYYY-MM-DDTHH:MM | null — если указан интервал",
     "category": "home|child|couple|trip|general",
     "who": "кого касается или null",
     "note": "адрес, кабинет, врач, телефон, номер брони — полезные детали, или null"}}
 ],
 "reply": "короткая дружелюбная фраза на русском"
}}
Правила:
- is_event = false и пустой events, если события с датой в сообщении нет (реклама, переписка,
  новость, просто информация) — такое я разберу обычным путём.
- Каждое отдельное событие — свой объект.
- Дата есть, времени нет — when без T. Года нет — бери ближайший будущий.
- Имя специалиста, адрес, кабинет и телефон клади в note, не в title. title без эмодзи.
- Ничего не выдумывай: чего в тексте нет — то null."""


DRAFT_PROMPT = """Ты помощник семейного календаря. Есть ЧЕРНОВИК события(ий), распознанный с фото и ещё НЕ добавленный в календарь:
{draft}

Сейчас: {now} (МСК). Пользователь пишет: {message}

Определи, что он хочет:
- ПРАВКА черновика («в 19:00», «назови день рождения Пети», «дата 5 августа», «убери адрес», «категория ребёнок») → op "edit", верни ПОЛНЫЙ обновлённый массив events (все события, не только изменённое)
- СОГЛАСИЕ добавить («да», «добавляй», «ок», «верно», «подтверждаю») → op "confirm"
- ОТКАЗ («нет», «не надо», «отмена», «убери») → op "cancel"
- сообщение ВООБЩЕ НЕ про черновик (другая просьба календарю) → op "other"

Верни ТОЛЬКО валидный JSON:
{{"op": "edit|confirm|cancel|other",
 "events": [{{"title": "...", "when": "YYYY-MM-DDTHH:MM | YYYY-MM-DD | null",
   "when_end": "YYYY-MM-DDTHH:MM | null (конец, если указан интервал)",
   "category": "home|child|couple|trip|general", "who": "... или null", "note": "... или null"}}],
 "reply": "короткая дружелюбная фраза на русском"}}
При op "edit" меняй только то, о чём попросили, остальные поля сохраняй как в черновике."""
