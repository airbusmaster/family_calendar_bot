# обязательное
requests>=2.31

# голосовые (локальное распознавание, данные не покидают сервер)
faster-whisper>=1.0

# синк с календарём iCloud (необязательно — без этих пакетов бот работает без синка)
caldav>=1.3
icalendar>=5.0
